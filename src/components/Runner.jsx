import React, { useEffect, useState } from 'react';

function Runner(props) {


 return (
    <>
    {/* <div className="min-h-screen flex items-center justify-center bg-gray-100">
    <div className="w-full max-w-md p-8 space-y-6 bg-white rounded-lg shadow-md">
        <p>
             {props.receivedData && (
        <p>
          <h2>Received Data:</h2>
          <ul>
          <li>Name:{props.receivedData.name}</li>
          <li>Location:{props.receivedData.address}</li>
          <li>Pickup Date:{props.receivedData.pickupDate}</li>
          <li>Instructions:{props.receivedData.instructions}</li>
          </ul>
        </p>
      )}
      </p>

      <p>
      {!props.receivedData && (
        <p>
          <h2>No pickUp requests received....</h2>
        </p>
      )}
      </p>
    </div>
    </div> */}
    </>
 )

}

export default Runner
